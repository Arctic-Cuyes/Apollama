import 'package:flutter/material.dart';

class ConnectionWarningCard extends StatelessWidget {
  const ConnectionWarningCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
